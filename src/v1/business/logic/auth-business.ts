import { AuthResponse } from "../../adapters/response";

export class AuthBusiness {
    public async login(dto: AuthDto): Promise<AuthResponse> {
        try {
            return await  new throw("not implemented")
        } catch (error) {
            
        }
    }
}