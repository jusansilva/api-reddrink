import { AuthRequest } from "../../../adapters/request";
import { AuthResponse } from "../../../adapters/response";

export class AuthController {
    
    public async login(req: AuthRequest, res: AuthResponse){
        try {
            const auth = 
        } catch (error) {
            
        }
    }
}